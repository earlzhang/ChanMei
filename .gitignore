# === 只分享结果，不分享代码 ===
# 白名单模式：默认忽略一切，仅显式保留结果与报告文件
# 新增的代码/配置文件会自动被忽略，无需手动维护

# 默认忽略所有文件与目录
*

# 保留：项目说明与本规则文件
!README.md
!.gitignore

# 保留：结果数据（data/ 下仅 results.csv 与 detail.md）
!data/
!data/results.csv
!data/detail.md

# 保留：交互式报告（reports/ 下仅 index.html 与 report_data.js）
!reports/
!reports/index.html
!reports/report_data.js

# data/ 下显式忽略派生/临时文件（覆盖 !data/ 带来的默认不忽略）
data/summary.json
